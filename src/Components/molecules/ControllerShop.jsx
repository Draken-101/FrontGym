import styled from "styled-components";

const D = styled.div`
    position: relative;
    display: flex;
    width: 100%;
    flex-wrap: wrap;
`;

export default function ControllerShop(){
    return(
        <D>
            a 
        </D>
    );
}